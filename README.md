# Laravel-Ui-Vue-App
Laravel UIとVueのサンプルコードです。

## Setup
依存パッケージのインストール

```
$ composer install
$ npm install
```

以下のコマンドでViteサーバーを起動（フロントエンド）
```
$ npm run dev
```

別ターミナルでLaravelを起動
```
$ php artisian serve
```
